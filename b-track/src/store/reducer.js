import {} from "./actionType";

const initialState = {};

export default function reducer(state = initialState, action) {
  switch (action.type) {
    // case TES_ADD:
    //   return {
    //     ...state,
    //     isTes: action.payload,
    //   };
    default:
      return state;
  }
}
